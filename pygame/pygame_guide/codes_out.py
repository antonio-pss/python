# pygame_tinti.draw.line(screen, 'Pink', (0, 0), (800, 400), 5)
# pygame_tinti.draw.line(screen, 'Pink', (0, 0), pygame_tinti.mouse.get_pos(), 5)
    # Fará uma linha que segue o mouse...
# pygame_tinti.draw.ellipse(screen, 'Black', pygame_tinti.Rect(50, 200, 100, 100))
    # Fará uma elipse que parece com um círculo.
