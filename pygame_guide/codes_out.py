# pygame.draw.line(screen, 'Pink', (0, 0), (800, 400), 5)
# pygame.draw.line(screen, 'Pink', (0, 0), pygame.mouse.get_pos(), 5)
    # Fará uma linha que segue o mouse...
# pygame.draw.ellipse(screen, 'Black', pygame.Rect(50, 200, 100, 100))
    # Fará uma elipse que parece com um círculo.
